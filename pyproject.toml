[project]
name = "germany-housing-monitor"
version = "0.1.0"
description = "Privacy-first, approval-gated housing discovery monitor for Germany"
requires-python = ">=3.11"
license = "MIT"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
